name: Broken Production Build

on: [push]

jobs:
  deploy-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Simulate Backend Crash
        run: |
          echo "🚀 Deploying v2.4.0 to production..."
          echo "🔌 Connecting to Postgres Primary..."
          sleep 2
          echo "❌ FATAL ERROR: Database connection refused. Too many active clients."
          exit 1  # This forces the build to FAIL
